<h1><%= t(".header") %> "<%= @post.title %>"</h1>
<%= monologue_admin_form_for ["admin", @post], multipart: true, method: :put do |post| %>
  <%= render partial: 'form', locals: {post: post} %>
<% end %>